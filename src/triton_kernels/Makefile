# Triton Kernels are JIT compiled by the Triton runtime.
# This Makefile is for informational purposes or future pre-compilation steps.

all:
	@echo "Triton kernels (Python) are ready for JIT compilation."

clean:
	rm -rf __pycache__
